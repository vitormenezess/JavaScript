/*const message = 'hello word'

console.log(`minha mensagem: ${message.toUpperCase()}`)

console.log(message.split(""))
console.log(message.length)
const names = ["Felipe", "João", "Julia", 10, false]

names.push(20,30, 'alyne')
names.unshift(2)
console.log(names)
names.pop()
console.log(names)
names[3] = "gustavo"
console.log(names)
const numbers = [1, 2, 3, 4, 5, 6];

console.log(
  numbers.map(function (number) {
    return number * 2;
  })
);

const ages = [9, 8, 13, 27, 30, 22, 40];

const evenAges = ages.filter(function (age){
    return age % 2 === 0;
})
 const sumOfAges = ages.reduce(function (age, accumulator){
     return accumulator + age;
 }, 0);

 console.log(sumOfAges);
 
const person = {
  firstName: "Felipe",
  lastName: "Rocha",
  age: "30",
  hobbiees: ["assistir F1", " Jogar futebol", "Ler"],
  dog: {
    name: "Kika",
    age: 10,
  },
};

const firstName = person.firstName;
const lastName = person.lastName;
const age = person.age;
const hobbiees = person.hobbiees;


const { firstName, lastName, age, hobbiees, dog:{name: dogName}, } = person;

console.log(firstName);
console.log(lastName);
console.log(age);
console.log(hobbiees);
console.log(dogName);

const todos = [
  {
    id: 1,
    description: "estudar Programção",
    isCompleted: false,
  },
  {
    id: 2,
    description: "Ler",
    isCompleted: true,
  },
  {
    id: 3,
    description: "Treinar",
    isCompleted: true,
  },
];

const todosJSON = JSON.stringify(todos);

const todosList = JSON.parse(todosJSON);
console.log(todosList)
*/